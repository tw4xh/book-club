# Always use the public npm registry for this project so installs work anywhere
# (Vercel, CI, other machines) and don't leak into a private/internal registry
# that would require authentication.
registry=https://registry.npmjs.org/
